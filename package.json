{
  "name": "mokelay-website",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt generate",
    "preview": "nuxt preview",
    "prepare": "nuxt prepare",
    "typecheck": "nuxt typecheck",
    "test": "vitest run",
    "test:unit": "vitest run",
    "e2e": "playwright test",
    "test:e2e": "playwright test",
    "test:all": "npm run test:unit && npm run test:e2e",
    "deploy:check": "npm run typecheck && npm run test:unit && npm run build"
  },
  "dependencies": {
    "nuxt": "^4.4.4",
    "vue": "^3.5.22"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "@types/node": "^25.6.0",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4",
    "vue-tsc": "^3.1.1"
  }
}
